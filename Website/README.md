# React + TypeScript + Vite

Minimal Vite template with React, TypeScript and HMR.

Official plugins:
- @vitejs/plugin-react (Babel)
- @vitejs/plugin-react-swc (SWC)

React Compiler is disabled by default.

For production builds, enable type-aware ESLint rules (use the type-checked configs).
